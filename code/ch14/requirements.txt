# AI Performance Engineering - Blackwell Requirements
# Target: Blackwell B200 (SM100) and B300 (SM103) with PyTorch 2.9 (cu130),
# CUDA 13.0, Triton 3.5.x (or newer patch releases for SM103 fixes).

# Core PyTorch ecosystem (CUDA 13 wheels). Pin to cu130 index; wheel tags add +cu130 automatically.
--index-url https://download.pytorch.org/whl/cu130
--extra-index-url https://pypi.org/simple
torch==2.9.*
torchvision==0.24.*

# Optional: install upstream Triton if you author custom GPU kernels outside of
# TorchInductor (PyTorch ships a bundled runtime already). Prefer ≥3.5.1/post
# builds when targeting SM103 (Blackwell Ultra) due to 3.5.0 regressions.
triton==3.5.1

# Monitoring and system insight
nvidia-ml-py==12.560.30
psutil==6.1.0
GPUtil==1.4.0
py-cpuinfo==9.0.0

# Data processing stack
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
pillow==10.2.0

# Visualization & reporting
matplotlib==3.8.4
seaborn==0.13.2
tensorboard==2.16.2
wandb==0.17.0
plotly==5.18.0
bokeh==3.4.1
dash==2.16.1
