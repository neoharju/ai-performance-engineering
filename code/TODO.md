# TODO

- Replace the temporary `--allow-unsupported-compiler` NVCC flag in ch11/ch12 (Makefiles + CUDA extensions) once a CUDA-supported host compiler is installed (e.g., GCC 12) or after upgrading the CUDA toolkit.
